# Pendências de regra de negócio: spec-001

Lacunas encontradas durante a implementação (`/code-and-audit`). Nada aqui foi decidido nem
implementado por suposição. Resolver no fim da spec (ou via `/speckit-clarify`) e só então
implementar.

| ID | Origem | Pendência | Estado atual do código |
|---|---|---|---|
| P-001 | T005 (`Voto`) | Qual `Escolha` um voto **pendente** carrega? `data-model.md` só diz que `respondidoEm` é `null` enquanto pendente. | `Voto` é só o record; nenhum comportamento de "pendente" criado. |
| P-002 | T006 (`RelatorioVotacao`) | Tipo e formato de `percentualSim` (int, double, arredondamento). `data-model.md` cita o campo como "derivado, apenas para exibição" sem tipo. FR-010 exige a porcentagem no relatório. | Campo **omitido** do record. Adicionar quando definido. |
| P-003 | T003/T004 (`RegraDeQuorum`) | Comportamento com `totalVotantes = 0`. Spec e D6 não cobrem. | Segue a fórmula do D6 (retorna `false`), sem teste. Decisão do Breno: seguir a fórmula por ora. |
| P-004 | FR-018 (novo) | "Excluir quem pediu o ensaio": o bot pode ter **mais de um** número admin (`AppProperties.getAdminPhones()`, D3). Exclui só o admin que solicitou, ou todos os admins? O texto do FR-018 e a Assumption da spec dizem apenas "a líder". | `RelatorioVotacao` só valida a soma; quem calcula o total (`Ensaio`/`RehearsalVotingService`, T018/T027/T032) ainda não existe. |
| P-005 (RESOLVIDA em 2026-09-20: Postgres/JPA, ver D4 atualizado) | T007 (`Ensaio`) / D4 / T013 | Breno disse: "não haverá mais Sheets, quero colocar banco mesmo". Isso contradiz `research.md` D4 (manter Sheets atrás da porta; Postgres fora de escopo) e a T013 (`GoogleSheetsRehearsalAdapter`). Decidir: atualizar D4/plan/T013 para um adapter de banco (JPA/Postgres) e rever o mapeamento do `data-model.md` (§ Mapeamento cita `Rehearsal`/`ResponseRecord` do Sheets). | Nada alterado no plan/tasks. `Ensaio.dataHora` é `String` sem formato do Sheets; o domínio não depende de Sheets. |
| P-006 | T009 / contracts | `contracts/rehearsal-ports.md` (in-port `RehearsalVotingUseCase`) declara `dataHora` como `LocalDateTime`, mas você decidiu `String` em `Ensaio` (T007). Alinhar o contrato antes das tasks T021/T050. | Só afeta o in-port (ainda não criado); `Ensaio` usa `String`. |
| P-007 | T009 (`RehearsalRepositoryPort`) | O contrato não diz o que `salvar` faz com `Voto`s: `Ensaio` ainda não guarda votos (entram na T030) e o `data-model.md` mapeia `Voto` para `ResponseRecord` do Sheets (ver P-005). | Porta criada exatamente como no contrato, sem tratar votos. |
| P-008 | T014 (`notificarLider`) | Líder = `getPrimaryAdminPhone()` (primeiro admin) por decisão do Breno; ligado à P-004 (vários admins). | Implementado assim. |
| P-009 | T016 (`isLider`) | `isLider` usa só `adminPhones` (decisão do Breno, alinhado à T016/D3). O legado `ActionDispatcher.isAdmin()` também aceita `Member.admin`: quem é admin só por esse campo não é líder no fluxo novo. Revisar na migração (T023/T051) e na P-005. | Implementado assim. |
| P-010 | T014 / AGENTS.md | Legado `sendTextMessageSeries` usa delay de 15000ms; AGENTS.md e T014 dizem 20s. O adapter novo usa 20s; o legado não foi alterado. Alinhar o legado quando migrar. | Adapter em 20s. |
| P-011 | T017 / T013 | A Phase 2 (Foundational) "bloqueia todas as User Stories", mas a T013 segue pendente (P-005). Breno mandou seguir para a T017 mesmo assim. | T017 usa só fakes das portas; não depende da T013. |
| P-012 | T017 | Pasta da exceção: Breno pediu "a pasta de exceptions"; criada como `com.jansen.bot.exception` (irmã de `client`, `config`...). O plan.md não cita pacote de exceções. | `NaoAutorizadoException` nesse pacote. |
| P-013 | T013 | Criei `Ensaio.reconstituir(...)` (fábrica pública para reconstruir um Ensaio existente, sem regras de criação). Nenhum documento a define; é necessária para o adapter ler do banco. Conferir o nome e se deve ser pública. | Implementado. |
| P-014 | T013 | O `docker-compose.yml` original tinha `postgres-bot` (postgres/postgres) compartilhando o volume `postgres_data` com o Postgres da Evolution. Passou a ter volume próprio (`postgres_bot_data`), credenciais via `.env` (`BOT_DB_NAME/USER/PASSWORD`) e healthcheck. Dados que já estivessem nesse volume não migram. `BOT_DB_PASSWORD` é obrigatória no `.env`. | Implementado. |
| P-015 | T013 | `pom.xml`: adicionado `flyway-database-postgresql` (Flyway 10+ não suporta Postgres só com o core) e `testcontainers.version=1.21.4` (o 1.20.4 do Spring Boot 3.4.1 não conversa com Docker 29+). | Implementado. |
| P-016 | T013 / T017 | Desde a T017, `RehearsalVotingServiceTest` não compila (falta `RehearsalVotingService`, T021), o que impede `mvn test` inteiro. Para validar a T013 tirei esse arquivo de `test/` só durante a execução e o devolvi. Só volta a compilar na T021. | Arquivo restaurado; suíte só roda inteira após a T021. |
| P-017 | T018 | Nova porta `IntegranteRepositoryPort` (`buscarTelefonesElegiveis()`), criada por decisão do Breno; não está no contrato nem no plan. Falta (a) documentar no `contracts/rehearsal-ports.md`/plan e (b) uma **task nova** para o adapter real: ler `Members` do Sheets (`GoogleSheetsRepository.findAllMembers`), filtrando ativos e sem "projeção" (regra de `RehearsalService.checkAllResponded`). Sem esse adapter o service não tem bean para injetar. | Só a interface e fake de teste. |
| P-018 | T018 | O legado filtra destinatários também por **tipo de ensaio** (vozes / instrumental / geral) em `ActionDispatcher.filterByRehearsalType`. A spec-001 não menciona tipos ("todos os integrantes"); o fluxo novo não tem esse filtro. Decidir se a spec deve cobrir isso antes da migração da T023. | Não implementado. |
| P-019 | T018 | Texto exato da mensagem de pedido de confirmação (FR-003) não está definido. O teste só exige data/hora e "sim"/"não"; o texto final (base sugerida: o do legado, "Ensaio marcado! ... Responde SIM ou NÃO") se define na T021. | Definir na T021. |
| P-020 | T017/T018 | O construtor de `RehearsalVotingService` passou a ter 5 portas (repositório, notificação, relógio, política de líder, integrantes); a T021 lista só 3. Ajustei o `RehearsalVotingServiceTest` da T017 (setUp) para isso. | Ajustado no teste. |

